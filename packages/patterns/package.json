{
  "name": "@devngn/patterns",
  "version": "0.0.0",
  "private": true,
  "description": "AI pattern intelligence database, trend metadata, and recognition engine for devngn.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --sourcemap --clean",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@devngn/core": "workspace:*",
    "zod": "^4.1.13"
  }
}
