import type { Config, Context } from "@netlify/functions";

/** Modern Netlify Request/Response function contracts re-exported for the site entry. */
export type NetlifyFunctionConfig = Config;
export type NetlifyFunctionContext = Context;
