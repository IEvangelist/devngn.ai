[build]
  command = "pnpm --filter @devngn/site... build"
  publish = "apps/site/dist"

[build.environment]
  NODE_VERSION = "24"

[functions]
  directory = "apps/site/netlify/functions"
  node_bundler = "esbuild"
