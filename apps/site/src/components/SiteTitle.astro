---
// Starlight override: brand mark matching the marketing shell's SiteHeader.
---

<a class="sl-site-brand" href="/">devngn<span class="dot">.</span>ai</a>

<style>
  .sl-site-brand {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: 1.25rem;
    letter-spacing: -0.03em;
    text-decoration: none;
    color: var(--ink);
    white-space: nowrap;
    line-height: 1;
  }
  .sl-site-brand .dot {
    color: var(--accent);
  }
</style>
