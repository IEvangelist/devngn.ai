---
// Starlight Footer override: keep Starlight's default footer (pagination,
// last-updated, edit link) and append the shared site footer so docs pages end
// the same way as the marketing pages.
import Default from "@astrojs/starlight/components/Footer.astro";
import SiteFooter from "./SiteFooter.astro";
---

<Default><slot /></Default>
<SiteFooter />
